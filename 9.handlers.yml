- name: demo on handlers
  hosts: localhost
  connection: local
  tasks:
    - name: list
      ansible.builtin.shell: ls

  handlers:
    - name: say hello
      ansible.builtin.debug:
        msg: hello, i am notify
